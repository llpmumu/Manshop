<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.manshop.android">

    <!-- 联网权限 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!-- if you want to load images from a file OR from the internet -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.view.WelcomeActivity"></activity>
        <activity
            android:name=".ui.view.MainActivity"
            android:label="@string/app_name"></activity>
        <activity android:name=".ui.view.LoginActivity" />
        <activity android:name=".ui.view.RegisterActivity" />
        <activity android:name=".ui.view.SearchActivity" />
        <activity android:name=".ui.view.PublishActivity" />
        <activity android:name=".ui.view.AddressActivity" />
        <activity android:name=".ui.view.GoodDetailActivity">

        </activity>
        <activity android:name=".ui.view.ComicActivity" />
        <activity android:name=".ui.view.EditAddressActivity" />
        <activity android:name=".ui.view.DialogueActivity" />
        <activity android:name=".ui.view.NewPublishActivity" />
        <activity android:name=".ui.view.OrderActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>